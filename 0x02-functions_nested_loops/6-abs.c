#include "main.h"

/**
 * _abs - get the absluote of the number
 *
 * @n: the number that u want to get its absluote
 * Return: the absluote of n
 *
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	return (n);
}
